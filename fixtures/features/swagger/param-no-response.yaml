swagger: '2.0'
info:
  title: Parameters without response
  version: '1.0'
paths:
  '/test':
    post:
      parameters:
        - name: title
          in: body
          description: Body argument
          required: true
          schema:
            type: string
