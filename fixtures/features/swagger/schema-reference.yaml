swagger: '2.0'
paths:
  '/test':
    get:
      produces:
        - application/json
      responses:
        200:
          schema:
            $ref: '#/definitions/Item'
definitions:
  Item:
    type: object
    properties:
      id:
        type: number
      name:
        type: string
