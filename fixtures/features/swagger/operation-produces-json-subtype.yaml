swagger: '2.0'
info:
  title: Produces JSON with subtype
  version: '1.0'
paths:
  '/test':
    get:
      produces:
        - application/hal+json
      responses:
        200:
          description: 'My Response'
          schema:
            type: object
            properties:
              name:
                type: string
