openapi: "3.0.0"
info:
  version: 1.0.0
  title: Response Components
paths:
  /user:
    get:
      summary: View the current User
      responses:
        '200':
          description: hi
components:
  responses:
    UserResponse:
      description: A user
      content:
        application/json:
          schema:
            type: object
