swagger: '2.0'
info:
  title: Default Response
  version: '1.0'
paths:
  '/test':
    get:
      responses:
        200:
          description: 200 response
        default:
          description: Default response
          schema:
            type: string
