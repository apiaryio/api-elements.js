{
  "name": "fury",
  "description": "API Description SDK",
  "version": "0.7.2",
  "author": "Apiary.io <support@apiary.io>",
  "main": "./lib/fury",
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:apiaryio/fury.git"
  },
  "engines": {
    "node": ">= 0.10.x"
  },
  "scripts": {
    "compile": "coffee -b -c -o lib/ src/ && cpy 'src/**/*.js' lib",
    "precoverage": "npm run compile",
    "coverage": "istanbul cover _mocha -- --compilers coffee:coffee-script/register -R spec --recursive",
    "precoveralls": "npm run coverage",
    "coveralls": "coveralls <coverage/lcov.info",
    "pretest": "npm run compile",
    "test": "mocha --compilers coffee:coffee-script/register -R spec --recursive",
    "prepublish": "npm run compile"
  },
  "dependencies": {
    "apiary-blueprint-parser": "",
    "drafter": "^0.2.5",
    "robotskirt": "",
    "sanitizer": "",
    "underscore": ""
  },
  "devDependencies": {
    "async": "~0.9.0",
    "chai": "~2.1",
    "coffee-script": "~1.9",
    "coveralls": "^2.11.2",
    "cpy": "^3.1.0",
    "istanbul": "^0.3.13",
    "mocha": "~2.1"
  },
  "license": "MIT"
}
