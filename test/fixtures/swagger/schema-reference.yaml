swagger: '2.0'
info:
  title: Schema Reference
  version: '1.0'
paths:
  '/test':
    get:
      produces:
        - application/json
      responses:
        200:
          description: Test description
          schema:
            $ref: '#/definitions/Item'
definitions:
  Item:
    type: object
    properties:
      id:
        type: number
      name:
        type: string
